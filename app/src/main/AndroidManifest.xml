<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.here.name.website.poom">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service android:name=".Utils.FirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <activity android:name="com.here.name.website.poom.Home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.here.name.website.poom.Requests.RequestsActivity"></activity>
        <activity android:name="com.here.name.website.poom.Profile.ProfileActivity">
            <intent-filter>
                <action android:name="com.here.name.website.poom_TARGET_NOTIFICATION"></action>
                <category android:name="android.intent.category.DEFAULT"></category>
            </intent-filter>
        </activity>
        <activity android:name="com.here.name.website.poom.Search.SearchActivity"
            android:windowSoftInputMode="adjustNothing">
        </activity>
        <activity android:name="com.here.name.website.poom.Share.ShareActivity"></activity>
        <activity android:name="com.here.name.website.poom.Profile.AccountSettingsActivity"></activity>
        <activity android:name="com.here.name.website.poom.Login.RegisterActivity"></activity>
        <activity android:name="com.here.name.website.poom.Login.LoginActivity"></activity>
        <activity android:name="com.here.name.website.poom.Share.NextActivity"></activity>

        <!-- For profile photo crop
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/> //optional (needed if default theme has no action bar) -->
    </application>

</manifest>